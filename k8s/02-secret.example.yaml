# TEMPLATE ONLY -- do not fill this in and commit it. Copy to secret.yaml
# (already gitignored, see step.md) or, preferably, create the Secret
# imperatively with `kubectl create secret generic` as shown in step.md so
# real values never touch disk as YAML.
apiVersion: v1
kind: Secret
metadata:
  name: km-backend-secret
  namespace: km-agent
type: Opaque
stringData:
  OPENAI_API_KEY: ""
  AZURE_OPENAI_API_KEY: ""
  AZURE_OPENAI_ENDPOINT: ""
  AZURE_OPENAI_CHAT_DEPLOYMENT: ""
  AZURE_OPENAI_EMBEDDING_DEPLOYMENT: ""
  ANTHROPIC_API_KEY: ""
  GEMINI_API_KEY: ""
  GROQ_API_KEY: ""
  JWT_SECRET: "REPLACE_WITH_STRONG_RANDOM_SECRET"
  DEMO_VIEWER_PASSWORD: "REPLACE_ME"
  DEMO_AUTHOR_PASSWORD: "REPLACE_ME"
